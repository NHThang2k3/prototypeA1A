Date: 4/12/2025

Một số công việc
Phân quyền
- Tạo 1 bộ master data cho factory
- Tạo 1 bộ master data cho department
- Tạo 1 bộ master data cho role
- Tạo user, gán user vào role

Đang setup server: gitlab
Database: Đang clone 1 phần data của họ (D-production) map vào database dev
Tech lead tạo ra Database, thảo luận với A1A để A1A follow
Cần đặt tên cho DB:
Tech lead sẽ thống nhất schema, các cột nào cần thiết
Tech lead cần ước tính tổng quan cho PM
Tech lead làm việc internal và external (Ân 1994: tech lead A1A)
Dev: Factory and Warehouse
Có 2 môi trường: dev và test



Quy trình làm việc
Cơ chế 1 cửa: Dev <--> Tech lead <--> BA <--> PO
Vấn đề deadline: PO, BA nói chuyện với tech lead, không nói chuyện với DEV
Tech lead quản lý FE,BE Dev internal và external, có quyền dí PO
PO quản lý BA
Báo cáo tiến độ: 1 tuần 1 lần ngẫu nhiên với PM
Trong buổi họp thì Tech lead làm host
Có 1 nhóm chỉ có Dev, anh Tony Toàn tạo



Date: December 4, 2025

Tasks Overview
Permissions / Authorization

Create a master data set for the Factory.

Create a master data set for the Department.

Create a master data set for the Role.

Create users and assign them to roles.

System Setup & Database
Server Setup: GitLab is currently being set up.

Database: Currently cloning partial data from the client (D-production) and mapping it to the Dev database.

Database Creation: The Tech Lead creates the Database and discusses it with A1A for them to follow.

Naming: A name is required for the Database.

Schema: The Tech Lead will finalize the schema and necessary columns.

Estimation: The Tech Lead needs to provide a high-level estimation for the Project Manager (PM).

Coordination: The Tech Lead works with both internal and external teams (Note: Mr. An [1994] is the Tech Lead for A1A).

Development Scope: Factory and Warehouse.

Environments: Two environments available: Dev and Test.

Working Process / Workflow
Single Point of Contact: Dev <--> Tech Lead <--> BA <--> PO.

Deadlines: The PO and BA communicate regarding deadlines with the Tech Lead only, not directly with the Developers.

Management:

The Tech Lead manages both Internal and External FE/BE Developers and has the authority to push/press the PO regarding requirements or blockers.

The PO manages the BA.

Progress Reporting: Once a week (randomly scheduled) with the PM.

Meeting Roles: The Tech Lead acts as the host during meetings.

Communication Channels: There is a "Developers only" group created by Mr. Tony Toan.


Date: 5/12/2025

"InlineFBRollDataDtl → chi tiết từng cây vải (mã cây, màu, khổ, mét, lot). Đây là ""master"" cho Roll.

InlineFBRollRelx → liên kết cây vải với các phiếu giao/phiếu xuất khác.

InlineFBRollOutput → xuất kho (khi phát vải sang bộ phận cắt).

InlineFBRollInspt → kết quả kiểm vải (inspection result cho từng cây).

InlineFBRollInsptDefct → chi tiết lỗi vải (nếu có, gắn với Inspt).

InlineFBRollLocHis → lịch sử vị trí cây vải trong kho (di chuyển từ kệ này qua kệ khác).

InlineFBWHPkList → danh sách xuất kho (pick list).

👉 Luồng: Nhập cây vải → Kiểm → Lưu kho → Xuất kho theo pick list → Ghi log lịch sử di chuyển."

"InlineFBWHPkList → liên quan đến phiếu xuất để QC có thể đối chiếu.

InlineFBRollDataDtl → cây vải gốc.

InlineFBRollInspt → thông tin kiểm vải (pass/fail, grade).

InlineFBRollInsptDefct → chi tiết lỗi từng cây.

InlineFBDefect → danh mục lỗi (master defect list).

👉 Luồng: Khi nhập kho, QC ghi nhận lỗi theo danh mục → kết quả gắn vào RollInspt → ảnh hưởng tới việc duyệt/cho phép xuất kho."

"CuttingPlanning → kế hoạch cắt (nhu cầu vải cho chuyền/bộ phận cắt).

InlineFBRollOutput → xuất kho cho lệnh cắt.

InlineFBWHPkList → pick list để gom vải cho kế hoạch.

InlineFBRollRelx → mapping cây vải nào đã cấp phát cho kế hoạch nào.

👉 Luồng: Kế hoạch cắt tạo nhu cầu → Pick list xuất kho → Output cấp phát → Relx ghi nhận cây nào đã phát → hiển thị Kanban số lượng còn/đã phát."


Date: 6/12/2025

https://github.com/AGWA/git-crypt/releases/tag/0.7.0

git crypt init

.gitattributes
server.js filter=git-crypt diff=git-crypt
src/** filter=git-crypt diff=git-crypt

Kiểm tra và Commit: git crypt status

Mở khóa cho đồng đội:
Xuất khóa ra file: git crypt export-key /path/to/my-secret-key.key
Gửi file my-secret-key.key này cho đồng nghiệp
Đồng nghiệp tải code về, sau đó chạy lệnh mở khóa: git crypt unlock /path/to/my-secret-key.key


Vào trang 
Scan barcode:
hiển thị thông tin: JOB NO, PO NO, Bundle NO, đang bị cái gì, recomend, record date, đã bao lâu chưa sửa, ngày sửa, status (chưa sửa, đang sửa, đã sửa), thời gian ghi nhận đang sửa, thời gian sửa xong,thời gian sửa (= thời gian sửa xong - thời gian ghi nhận đang sửa)

list

update:
create: ghi description, đề nghị làm gì, upload hình chụp, bấm create


sửa lại trang Repair Request List: 
Bảng hiển thị thông tin: JOB NO, PO NO, Bundle NO, đang bị cái gì, đề nghị làm gì, status ( chưa sửa, đang sửa, đã sửa), record date (thời gian ghi nhận),  thời gian ghi nhận đang sửa, thời gian sửa xong, thời gian sửa (= thời gian sửa xong - thời gian ghi nhận đang sửa)
Đổi nút Create New Request thành Scan barcode, khi ấn vào Scan barcode sẽ hiện giao diện scan barcode, giả lập scan barcode thành công, scan xong sẽ hiện thị 1 form để tạo request repair, form có các thông tin: JOB NO, PO NO, Bundle NO, đang bị cái gì, đề nghị làm gì, upload hình chụp hoặc chụp trực tiếp, bấm create để tạo request repair

Khi mở giao diện scan barcode, cho người dùng chọn 3 kiểu scan khác nhau:
scan tạo request repair
scan cập nhật request repair: scan sẽ hiển thị các thông tin của request repair, người dùng chỉ cần ấn bắt đầu sửa để bắt đầu sửa.
scan hoàn thành request repair: scan sẽ hiển thị các thông tin của request repair, người dùng chỉ cần ấn hoàn thành để hoàn thành request repair.

trong bảng repair request list, bỏ tùy chọn update status trong action, khi ấn vào view details sẽ hiển thị các thông tin của request repair. điều chỉnh giao diện và dữ liệu thành tiếng anh.


==== MoM 9/12 ====

- Khi người dùng ở tab nào (fabric, accessory, packaging), thì chỉ cho phép add location với purpose đó, phần purpose đổi từ dropdown thành text.
- add location: phần warehouse biến thành dropdown để chọn warehouse.
- chuyển nút create warehouse và nút add location xuống phần chọn tab fabric, accessory, packaging.

- thêm tính năng sửa, xóa, enabled/disabled warehouse.

- không cần hiển thị state/location cho warehouse. 
- đảm bảo đồng nhất cột với location nữa, nghĩa là cột QR status thì warehouse sẽ trống.
- chỉ cho thanh đổi trạng thái enabled/disabled khi người dùng ấn vào edit, không cho thay đổi ở ngoài.
- không cho edit purpose trong edit warehouse.

- chuyển nút create warehouse lên trên bên phải dropdown chọn factory.
- Purpose trong form create new warehouse cho chọn 4 option: fabric, accessory, packaging, temp warehouse.

- hiện thị thông tin enabled/disabled của warehouse ra ngoài trong tab location.

- Rule: chỉ được disabled warehouse hoặc location khi occupancy = 0. nếu vi phạm sẽ thông báo lỗi.
- trong add location form, dropdown warehouse chỉ hiển thị danh sách warehouse đang có trong hệ thống. 

- Rule: chỉ được delete warehouse hoặc location khi occupancy = 0. nếu vi phạm sẽ thông báo lỗi.


FAC_Location Management_Create new warehouse
FAC_Location Management_View warehouse list
FAC_Location Management_Update warehouse information
FAC_Location Management_Delete warehouse

==== MoM 10/12 ====

total yard: > 1000, check 3%, nếu 3% bị lỗi thì check tiếp 3% khác.
total yard: < 1000, check 100%

Không cho A1A chọt vào database, nhưng sẽ cung cấp API cho A1A để sử dụng.



==== MoM 11/12 ====
define mã lỗi và message trả về.

Trong layout decoration, ở mục Dashboard decoration, thêm các tab con sau:
- Bonding Dashboard
- Embroidery Dashboard
- Pad-Printing Dashboard
- Heat Press Dashboard
Mỗi tab tương ứng với 1 màn hình dashboard riêng. màn hình dashboard sẽ tương tự file DecorationDashboardPage.tsx những thông tin trong màn hình sẽ tương ứng dữ liệu của tab đó.

ở màn hình BufferScanOutPage.tsx trong thư mục decoration. sửa lại giao diện theo luồng nghiệp vụ sau:
- bắt đầu màn hình sẽ hiển thị giao diện scan barcode, giả lập ấn scan barcode với QC pass, màn hình sẽ thông báo thành công. giả lập ấn scan barcode với QC fail, màn hình sẽ thông báo thất bại với lí do QC fail.

giả lập ấn nút QC pass hoặc QC fail là chạy tiếp luôn, không cần ấn enter nữa.
chỉ để 1 nút QC fail thôi, không cần 3 nút


sửa lại: màn hình BufferScanOutPage là màn hình dành cho tablet, mobile. 

sẽ có 1 nút, khi ấn vào sẽ hiện popup Recent Scans

giao diện scan là màn hình camera để người dùng đặt barcode vào là chạy á

bỏ lựa chọn dùng manual đi 
